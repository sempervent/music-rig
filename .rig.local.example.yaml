# Copy to `.rig.local.yaml` (gitignored) and fill machine-local paths.
# Never commit absolute paths or secrets into canonical data/*.yaml.
# Null means "not configured on this machine."

paths:
  backup_root: null
  pfl_jam_ableton_set: null
  obs_export: null
  stream_deck_export: null
  kaoss_backup: null
  controller_mappings_export: null

# Optional agent provider (stdio command). Not required for packet/validate/apply.
# agent:
#   provider: command
#   argv:
#     - /path/to/agent-wrapper
#   timeout_seconds: 120
#   env_forward:
#     - MY_API_KEY_NAME_ONLY
#   max_context_rounds: 5
#   max_stdout_bytes: 1000000
